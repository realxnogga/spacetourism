

import { Header } from "../component/header";

export const Destination = () => {

    return (
        <section className="relative h-screen w-screen bg-cover bg-center flex items-center justify-center bg-[url('../assets/destination/background-destination-desktop.jpg')]">
            <Header />
            
            <p>Destination</p>
        </section>
    )
}